spring:
  cloud:
    config:
      name: application-fastDFSProducers,application-fastDFSs
      profile: dev
      discovery:
        serviceId: configServer
        enabled: true
      label: master

