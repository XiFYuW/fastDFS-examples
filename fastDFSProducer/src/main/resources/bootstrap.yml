spring:
  cloud:
    config:
      name: application-fastDFSProducer,application-fastDFS
      profile: dev
      discovery:
        serviceId: configServer
        enabled: true
      label: master

